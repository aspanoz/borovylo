# Расширение для reaper

## Requirement
* brew install wget

## Installation

* git clone xxx
* cd <PROJECT_NAME>
* git submodule update --init --recursive
**** mkdir build && cd build
* cmake ..
* make
